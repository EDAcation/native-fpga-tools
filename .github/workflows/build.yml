name: Build

on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        arch: [linux-x64]
        target: [yosys]

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - name: Build
        run: |
          cd oss-cad-suite-build
          ./builder.py build --arch=${{ matrix.arch }} --target=${{ matrix.target }}
      - name: List files
        run: find .
